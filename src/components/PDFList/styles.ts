import styled from "styled-components"

const Wrapper = styled.div`
  height: 100%;
  max-height: 1080px;

  gap: 1.5rem;
  display: grid;

  padding-right: 1rem;
  overflow-y: auto;
  overflow-x: hidden;
`

export default Wrapper
